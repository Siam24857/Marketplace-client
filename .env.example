# Production (Vercel client env)
NEXT_PUBLIC_API_URL=https://marketplace-server-tawny.vercel.app/api

# Google OAuth (server-side only — do NOT prefix with NEXT_PUBLIC)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Local dev fallback
# NEXT_PUBLIC_API_URL=http://localhost:5000/api
